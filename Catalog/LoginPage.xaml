<!--АВТОРИЗАЦИЯ-->

<Page
    x:Class="Catalog.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Catalog"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" MaxWidth="250">
            <Grid Padding="0 0 0 0">
                <Image Source="./Assets/logo.png" Margin="0,0,0,10"/>
            </Grid>


            <TextBlock>логин</TextBlock>
            <TextBox x:Name="Username" PlaceholderText="введите логин" BorderThickness="0.5" BorderBrush="Gray" Margin="0 0 0 8"></TextBox>

            <TextBlock>пароль</TextBlock>
            <PasswordBox x:Name="Password" PlaceholderText="введите пароль" BorderThickness="0.5" BorderBrush="Gray" Margin="0 2 0 8"></PasswordBox>

            <Button HorizontalAlignment="Stretch" Margin="0 10 0 0" Click="Login">Войти</Button>

            <TextBlock TextAlignment="Center" FontWeight="Normal" Margin="0 0 0 50" x:Name="Error"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
